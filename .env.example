# Portao do PROPRIO site (login do navegador). Defina na Vercel.
APP_USER=wt
APP_PASS=troque-esta-senha

# API do motor (Render) e seus segredos — SO no servidor (proxy), nunca no navegador.
RENDER_API_URL=https://crm-motor.onrender.com
RENDER_GATE_USER=wt
RENDER_GATE_PASS=coloque-a-senha-do-portao-do-motor
RENDER_USER_ID=1
